# Plan — X32 : ระบบผู้ช่วยวิเคราะห์การลงทุน (Investment Copilot)
> สถานะ: Active | อัปเดต: 2026-08-07

## 1. วัตถุประสงค์
- ช่วยผู้ใช้ (ผู้กำลังศึกษาการลงทุน ไม่มีพื้นฐานการเงินลึก) คัดกรองสินทรัพย์น่าลงทุนได้เองทุกวัน โดยไม่ต้องอ่านงบการเงินเป็น
- อธิบาย **เหตุผล** ว่าน่าลงทุนระยะสั้น/กลาง/ยาว/ปันผล เพราะอะไร ด้วยภาษาที่คนทั่วไปเข้าใจ พร้อมกราฟประกอบ
- รวมหุ้นไทย หุ้นต่างประเทศ ทอง กองทุนรวม ไว้ในที่เดียว พร้อมข่าวจากแหล่งน่าเชื่อถือ

## 2. ขอบเขต
- **ทำ:** เว็บส่วนตัวมีรหัสผ่าน deploy บน Netlify / Dashboard สรุปรายวัน / คัดกรองด้วย factor + technical / AI Agents วิเคราะห์และอธิบาย / หน้าถาม-ตอบ AI / backtest เกณฑ์คัดกรอง / UI ธีม Brutalism
- **ไม่ทำ:** ไม่ส่งคำสั่งซื้อขายจริง ไม่เชื่อมพอร์ตโบรกเกอร์ ไม่ทำ real-time streaming (อัปเดตวันละครั้ง) ไม่เปิดสาธารณะ

## 3. เกณฑ์ว่าเสร็จ (Definition of Done)
- [x] เข้าเว็บด้วยรหัสผ่านได้ / คนไม่มีรหัสเปิด JSON ตรงไม่ได้ (404) — ตรวจบน production 2026-08-07
- [ ] ข้อมูลอัปเดตอัตโนมัติทุกวันผ่าน GitHub Actions โดยไม่ต้องสั่งเอง — รอสร้าง GitHub repo
- [x] Dashboard บอกได้ว่าวันนี้อะไรมาแรง/น่าสนใจ ครบ 4 กลุ่มสินทรัพย์
- [x] กดดูสินทรัพย์รายตัว เห็นเหตุผลแยกระยะสั้น/กลาง/ยาว/ปันผล พร้อมกราฟ
- [ ] คำอธิบายทุกจุดอ่านเข้าใจได้โดยไม่ต้องมีพื้นฐานการเงิน
- [ ] ถาม AI ภาษาไทยแล้วได้คำตอบอ้างอิงข้อมูลจริงของวันนั้น
- [ ] มี backtest แสดงผลย้อนหลังของเกณฑ์คัดกรอง
- [x] ไม่มี API key ใดๆ อยู่ในโค้ดหรือ repo — สแกน staged files ก่อน commit แรกแล้ว

## 4. Tech Stack
- **Pipeline:** Python 3.11 + yfinance + pandas + anthropic — รันบน GitHub Actions (cron รายวัน)
- **Frontend:** static HTML/CSS/JS เขียนเอง ไม่มี build step + Chart.js ผ่าน CDN
- **Backend:** Netlify Functions (Node) — login / data / chat
- **Deploy:** GitHub repo → Netlify auto-deploy

## 5. ข้อจำกัดและข้อควรระวัง
- **Netlify ไม่รองรับ Python** → Python ต้องรันบน GitHub Actions เท่านั้น เขียนผลเป็น JSON
- **ไม่รับประกันผลตอบแทน** เป้าหมาย >10%/ปี ใช้เป็นเกณฑ์คัดกรองเท่านั้น ต้องมี disclaimer ทุกหน้า
- **กฎหมาย:** แนะนำการลงทุนต่อสาธารณะในไทยต้องมีใบอนุญาต ก.ล.ต. → เว็บต้องมีรหัสผ่านเสมอ ห้ามเปิดสาธารณะ
- **AI ห้ามคำนวณตัวเลขเอง** — ป้อนตัวเลขที่ Python คำนวณแล้วเข้าไปเป็น context กัน hallucination
- secret ทั้งหมด (`ANTHROPIC_API_KEY`, `SITE_PASSWORD`, `SEC_API_KEY`) อยู่ใน GitHub Secrets + Netlify env vars เท่านั้น
- yfinance เป็น API ไม่เป็นทางการ ต้องมี cache fallback เสมอ

## 6. Log การเปลี่ยนวัตถุประสงค์
- 2026-08-07: สร้างโปรเจกต์ (เดิมตั้งชื่อ X33 แล้วย้ายมาใช้โฟลเดอร์ X32 ที่ว่างอยู่)
